package controller.planets;

import javax.swing.ImageIcon;

import controller.Planets;

public class JavaScript extends Planets {

	public JavaScript(String name, int initialPositionX, int initialPositionY, int translationSpeed, double rotationSpeed, int maxMovement, ImageIcon icon) {
		super(name, initialPositionX, initialPositionY, translationSpeed, rotationSpeed, maxMovement, icon);
	}
}
